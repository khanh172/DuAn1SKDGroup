using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "StatConfig", menuName = "GameConfiguration/StatConfig", order = 1)]
public class stats2 : ScriptableObject
{
    public int HP;
    public int ATK;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
